import { parse } from "./parser";

describe('pdfParser dummy', () => {
  it.only('pdfParser', () => {
    expect(true).toBeTruthy();
  })
});

// describe('App', () => {
//   it.only('Get table from parsed file', () => {
//     const table = parse("test.pdf");
//     expect(table).not.toBeUndefined();
//   })
// });